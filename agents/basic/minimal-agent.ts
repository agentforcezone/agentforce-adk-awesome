import { AgentForceAgent } from '@agentforce/adk';

const agent = new AgentForceAgent({ name: "MinimalAgent" });

console.log("✅ AgentForce ADK installed successfully!");
console.log("Agent created:", agent);
